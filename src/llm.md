Nuxt UI is a collection of reusable components.

Use the tokens for every color/length unless not available.

Icons MUST use icones.js format `<collection>:<icon>` (e.g. "lucide:house", "heroicons:check-circle").